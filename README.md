# Boxy desktop auto-update feed

Signed macOS auto-update artifacts (`latest.json` + the `.app.tar.gz` tarball) for the Boxy Preview desktop app, published automatically by CI on each build. Not intended for manual download.
